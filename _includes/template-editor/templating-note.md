Many properties can use [Variables](/template-editor/variables/) to assign their value when each image is created.

Variables can be required, ignored, or given a fallback value. When you render the template, pass values with `template_values`. See [Variables](/template-editor/variables/) for the full flow.
