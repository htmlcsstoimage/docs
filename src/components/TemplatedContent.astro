<h2 id="templated-content">Templated content</h2>
<slot />
<p>Many properties can use <a href="/template-editor/variables/">variables</a> to assign their value when each image is created. Variables can be required, ignored, or given a fallback value. Pass values in <code>template_values</code> when rendering the template.</p>
